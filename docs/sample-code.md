# Code Samples

## TypeScript

```typescript
function greet(name: string): string {
  return `Hello, ${name}!`
}
```

## Python

```python
def fib(n: int) -> int:
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)
```

## Bash

```bash
npm run dev
```
