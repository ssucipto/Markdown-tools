# Task 24: View Source Toggle

**Milestone**: M4 | **Est**: 3h | **Depends**: task-7

## Objective
FR-2.9 — read-only raw markdown panel toggle.

## Steps
1. Toolbar toggle “View source” shows `<pre>` with raw markdown (read-only)
2. Toggle back to rendered view without losing scroll position where possible

## Acceptance
- [ ] User can view raw `.md` text without editing
