# Task 20: Folder Browser

**Milestone**: M4 | **Est**: 8h | **Depends**: task-14

## Objective
FR-1.4, FR-1.5 — File System Access API directory picker + sidebar file list.

## Steps
1. `showDirectoryPicker` when available; fallback to multiple file input
2. List `.md` files in sidebar; click loads file
3. Adapt logic from source `server/routes/api/docs.ts` (client-side only)

## Acceptance
- [ ] User can open a folder and switch between markdown files in sidebar
