# Task 21: True DOCX Export

**Milestone**: M4 | **Est**: 8h | **Depends**: task-13

## Objective
FR-5.6 — replace HTML-as-Word default with `.docx` (html-to-docx or docx library).

## Steps
1. Evaluate `html-to-docx` vs `docx` + HTML converter
2. Map h1–h6 to Word heading styles
3. Embed mermaid as PNG in docx; keep `.doc` as fallback export option

## Acceptance
- [ ] Downloaded `.docx` opens in Word with heading styles and diagrams
